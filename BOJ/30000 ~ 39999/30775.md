# [30775 : Рассадка](https://www.acmicpc.net/problem/30775)
https://www.acmicpc.net/problem/30775

## 문제
**문제**  
K개 학교의 학생들을 M반으로 최대한 인원이 같게 쪼갤 때, 학생이 가장 많은 반의 인원을 구하는 문제

## 아이디어
Arithmetic(Math)

K개 학생 전체 인원의 합을 구해 M반으로 나누는데, 나누어 떨어지지 않으면 각 반에 1명씩 더 추가가 될 수 있다.
```java
BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

StringTokenizer st = new StringTokenizer(br.readLine());
int M = Integer.parseInt(st.nextToken());
int K = Integer.parseInt(st.nextToken());
long sum = 0;
st = new StringTokenizer(br.readLine());
for (int i = 0; i < K; i++) {
    sum += Integer.parseInt(st.nextToken());
}
System.out.println((sum / M) + (sum % M == 0 ? 0 : 1));
```